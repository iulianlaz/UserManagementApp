/**
 * Handles the way a user is edited
 */
$(document).ready(function(){
    /* ============ Add user template =========== */
    $('#generalContainer').on('click', '#filterSubmitId',  buildUserList);
});